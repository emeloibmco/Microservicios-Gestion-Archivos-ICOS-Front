import { FilterSearchAssetPipe } from './filter-search-asset.pipe';

describe('FilterSearchAssetPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSearchAssetPipe();
    expect(pipe).toBeTruthy();
  });
});
